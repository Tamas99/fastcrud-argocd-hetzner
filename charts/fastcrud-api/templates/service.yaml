apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastcrud-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "fastcrud-api.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
